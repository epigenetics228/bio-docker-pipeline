# 🧬 Bio-Docker-Pipeline: Automated Variant Calling

Профессиональный биоинформатический конвейер для автоматического поиска генетических вариантов (Variant Calling). Система полностью контейнеризирована с помощью **Docker** и автоматизирована через **GitHub Actions**.

## 🚀 Основные возможности
* **Параллельный запуск**: Обработка нескольких образцов одновременно благодаря матричной стратегии GitHub.
* **Гибридная загрузка данных**: Автоматическое переключение между локальными файлами и загрузкой тяжелых данных из облака через `wget`.
* **Полный стек ПО**: Интеграция `fastp`, `bwa`, `samtools`, `bcftools` и `multiqc` в одном Docker-контейнере.
* **Интерактивные отчеты**: Автоматическая публикация сводных отчетов MultiQC на GitHub Pages.
* **Telegram Уведомления**: Мгновенные оповещения о статусе завершения анализа.

## 🛠 Стек технологий
* **Языки**: Bash, YAML, Dockerfile.
* **Инструменты**: Docker, GitHub Actions.
* **Биоинформатика**: BWA (выравнивание), SAMtools (манипуляции с BAM), BCFtools (поиск мутаций), MultiQC (контроль качества).



## 📋 Инструкция по настройке

### 1. Подготовка репозитория
Убедитесь, что в корне проекта находятся:
* `Dockerfile` — среда со всеми инструментами.
* `ref.fa` — эталонный геном (или настройте ссылку в Secrets).
* `.gitignore` — настроен для игнорирования тяжелых `.fq` и `.bam` файлов.

### 2. Настройка GitHub Secrets
Для работы облачной загрузки и уведомлений добавьте следующие секреты в **Settings > Secrets > Actions**:
* `URL_REFERENCE`: прямая ссылка на референсный геном.
* `URL_SAMPLE1` / `URL_SAMPLE2`: прямые ссылки на сырые чтения (.fq).
* `TELEGRAM_TOKEN` & `TELEGRAM_TO`: данные вашего бота для уведомлений.

### 3. Права доступа
Для успешной публикации отчетов в **Settings > Actions > General** необходимо установить **Read and write permissions**.

## 🧪 Пример использования
Проект был успешно протестирован на данных секвенирования нейронов (исследование эпигенетической модуляции синтеза норадреналина). Результаты доступны в виде аннотированных VCF-файлов в артефактах сборки.

---
**Разработчик**: [epigenetics228](https://github.com/epigenetics228)  
